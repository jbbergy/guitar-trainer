/**
 * UI Constants for Guitare Trainer Application
 * Defines minimum window constraints per specification
 */

export const MIN_WIDTH = 800
export const MIN_HEIGHT = 600

export const WINDOW_CONFIG = {
  minWidth: MIN_WIDTH,
  minHeight: MIN_HEIGHT,
  width: 1024,
  height: 768,
  backgroundColor: '#1a1a1a'
} as const
